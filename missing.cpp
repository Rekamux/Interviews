#include <stdio.h>
#include <stdlib.h>

typedef uint8_t u;

u missing( u *list, int n, int b )
{
    u mask = 1 << b;
    u  = 0;
    for( int i = 0; i < n; i++ )
    {
        u next = list[i] & mask;
        if( 
    }
}
